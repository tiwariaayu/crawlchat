{
  "name": "crawlchat-client",
  "version": "1.0.2",
  "description": "A CrawlChat utility package for Docusaurus integration",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node -e \"require('fs').chmodSync('dist/index.js', '755')\"",
    "prepare": "npm run build"
  },
  "author": "Pramod",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^19.2.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "react": "^16.8.0"
  }
}
